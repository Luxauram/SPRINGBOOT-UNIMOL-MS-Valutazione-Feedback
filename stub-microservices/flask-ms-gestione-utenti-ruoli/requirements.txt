# Flask Framework
Flask==3.1.1

# CORS support per chiamate cross-origin
Flask-CORS==4.0.0

# Gestione variabili d'ambiente (opzionale ma utile)
python-dotenv==1.0.0

# JSON handling avanzato (opzionale)
jsonschema==4.19.0

# HTTP client per test (opzionale)
requests==2.31.0

PyJWT==2.8.0